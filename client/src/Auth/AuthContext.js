/**
 * auth context used provide the app with current user data token
 */
import { createContext as CreateContext } from 'react';

const AuthContext = new CreateContext();
export default AuthContext;
